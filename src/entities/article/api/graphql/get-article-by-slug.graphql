query GetArticleBySlug($slug: String!) {
	getArticleBySlug(slug: $slug) {
		id
		title
		slug
		description
		contentJson
		contentHtml
		contentText
		tags
		coverImage
		published
		publishedAt
		readingTime
		views
		likes
		commentsCount
		createdAt
		updatedAt
		author {
			id
			displayName
			avatarUrl
		}
		category {
			id
			name
			slug
		}
		comments {
			id
			content
			createdAt
			updatedAt
			author {
				id
				displayName
				avatarUrl
			}
		}
	}
}
