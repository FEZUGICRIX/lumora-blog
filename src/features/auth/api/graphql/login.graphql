mutation Login($email: String!, $password: String!) {
	login(loginInput: { email: $email, password: $password }) {
		... on User {
			id
			email
			username
			displayName
			role
			emailVerified
		}
		... on MessageResponse {
			message
		}
	}
}
