mutation Login($email: String!, $password: String!, $code: String) {
	login(loginInput: { email: $email, password: $password, code: $code }) {
		... on UserProfile {
			id
			email
			username
			displayName
			role
			emailVerified
		}
		... on MessageResponse {
			message
		}
	}
}
